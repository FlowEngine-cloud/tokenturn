{
  "name": "@tokenturn/sdk",
  "version": "0.1.0",
  "description": "Tokenturn TypeScript SDK: wrap() your OpenAI/Anthropic client to count every call, track() outcomes with real value. Fail-open, zero runtime dependencies.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json"
  },
  "engines": {
    "node": ">=18.18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
